class ipa {
var prefix="/usr/local/";
var conf=$prefix."etc/ipa.conf";
var stat=$prefix."bin/ipastat";
var ipa=$prefix."bin/ipa";

  function add_user($username) {
  }

  function remove_user($username) {
  }
  
  function add_night_time($username) {
  }

  function remove_night_time($username) {
  }

  function restart_ipa() {
  }

  function add_simple_rule($name,$comment,$rule) {
  }

  function remove_simple_rule($name,$comment,$rule) {
  }


  function remove_nigth_simple_rule($name,$comment,$rule) {
  }
  

  function add_nigth_simple_rule($name,$comment,$rule) {
  }
  

}